# Copy to .env, chmod 600 .env, and set a unique long password before LAN access.
DASH_HOST=127.0.0.1
DASH_PORT=8765
DASH_PASSWORD=
DASH_CONFIG=./config.json
DASH_DATA_DIR=./data
# For a second PC running in agent mode (optional):
# DASH_AGENT_TOKEN=
